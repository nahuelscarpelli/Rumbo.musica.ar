export function GrainOverlay() {
  // Visual grain is applied globally via the `.grain-overlay` class on <body>.
  // This component is reserved for future per-section grain or extra noise layers.
  return null;
}
